services:
  - type: web
    name: dealpilot-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    buildContext: ./
    plan: starter
    region: oregon
    autoDeploy: true
    healthCheckPath: /health
